logs/
__pycache__/
*.pyc
local_backend_pids.json
preservation_client.exe

# Local cache artifacts recovered from a user's install. These are required for
# full static catalog/cosmetic data, but they are not redistributable repo
# source and must stay local.
cache/
reference_cache/

# Local workspace folders. Keep the actual client/game install beside this
# repository, not inside it.
Skate/
game/
artifacts/
knowledge/

# Generated/local runtime output.
*.log
*.jsonl
*.pid
*.tmp

# Original client/game content guardrails. The bundled Python runtime is already
# tracked, but new game-shaped binaries/data should not be added to this repo.
*.exe
*.dll
*.toc
*.cas
*.sb
*.big
*.dat
*.pak
*.ucas
*.utoc
*.chunk
*.bundle
*.rdata
*.res
*.bin

Core/
Data/
Support/
x64/
__Installer/
